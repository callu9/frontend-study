
# 기초 CS 질문

## 1. 데이터 구조와 알고리즘

### 1-1. 자료구조

#### 1. 배열과 연결 리스트의 차이점은 무엇인가요?

| 자료구조	| 특징 | 장점 | 단점 |
| --- | --- | --- | --- |
| 배열 | 메모리 상에 연속적으로 저장된다. | 인덱스를 통한 임의 접근(Random Access)가 O(1)로 매우 빠르다. | 크기가 고정되어 있거나, 크기 변경 시 전체 데이터를 재배열해야 할 수 있다. |
| 연결리스트 | 각 요소(노드)가 다음 요소에 대한 참조(포인터)를 가지고 있으며, 메모리 상에 산발적으로 저장된다. | 리스트의 시작과 중간 부분에서, 삽입과 삭제가 O(1)로 효율적이다. | 임의 접근이 불가능하여, 특정 인덱스에 접근하려면 순차적으로 탐색해야 하므로 O(n)의 시간복잡도를 가진다. |

#### 2. 스택(Stack)과 큐(Queue)의 차이와 각각의 사용 사례를 설명하세요.

| 자료구조	| 특징 | 사용 사례 |
| --- | --- | --- |
| 스택 | 마지막에 추가된 항목이 먼저 제거된다. <br /> 후입선출 (LIFO: Last In First Out) | 함수 호출 스택, undo/redo 기능, 괄호 검증 등 |
| 큐 | 가장 먼저 추가된 항목이 먼저 제거된다. <br /> 선입선출 (FIFO: First in First Out) | 작업 대기열, 프린터 큐, 너비 우선탐색 알고리즘 (BFS) 등 |

#### 3. 해시맵(HashMap)과 트리(Tree)의 주요 차이점은 무엇인가요?

| 자료구조	| 특징 | 장점 | 단점 |
| --- | --- | --- | --- |
| 해시맵 | 키-값 쌍으로 데이터를 저장한다. | 해시 함수를 사용한 검색, 삽입, 삭제가 평균적으로 O(1)로 가능하다. | 데이터의 순서가 보장되지 않으며, 해시 충돌 관리가 필요하다. |
| 트리 | 계층적 구조로 데이터를 저장한다.<br />(ex. 이진 탐색 트리(BST)는 정렬된 순서를 유지한다.) | 범위 검색이나 순회(traversal)에 유리하며, 평균 O(log n)의 시간 복잡도를 가진다. | 불균형이 발생하면 최악의 경우 O(n)이 될 수 있다. |

* HashMap과 Map의 차이점은? ❓
  - JS에서는 Object와 Map이 HashMap의 역할
	- Java에서는 HashMap이 Map 인터페이스(키와 값 쌍으로 데이터를 저장하는 구조)를 구현한 클래스이며, 키의 중복을 허용하지 않지만 값의 중복은 허용한다.

* 해시(Hash)란? ❓
	- 설명: 데이터를 다루는 기법으로, 키-값으로 매핑되는 과정 자체를 해싱(Hashing)이라 한다.
 	- 특징: 
		- 검색과 저장이 아주 빠르게 진행된다. 평균 시간 복잡도가 O(1)에 수렴 (key-value가 쌍으로 존재하며, key값이 인덱스로 변환되기 때문에)
		- 무결성: 같은 입력값에 대해 같은 출력값을 보장하며, 해시는 특정한 데이터를 상징하는 고정된 길이의 데이터로 변환하기 때문에, 원래의 데이터가 조금만 달라져도 확연하게 달라지는 특성을 가지고 있어 무결성을 지키는데 많은 도움을 준다.
		- 보안성: 복호화가 불가능한 단방향 암호화 기법의 특징을 가지고 있으며, 상대적으로 시스템 자원(CPU, 메모리) 소모가 덜하다.
 	- 해시 값이란? 다양한 길이를 가진 데이터를 **고정된 길이를 가진 데이터로 매핑**한 값
	- Hash function이란? 임의의 길이의 데이터를 고정된 길이의 데이터로 매핑하는 단방향성 함수/알고리즘이다. 알고리즘 예시로 MD5, SHA 함수 등이 있으며, 사용 예시로 세션 키 도출, 일회용 패스워드(OTP) 생성, 주요 정보의 무결성 확인 등이 있다.

* 해시 충돌(Hash Collision)이란? 서로 다른 문자열이 Hash function을 통해 Hash한 결과가 같은 경우(중복되는 경우)를 말하며, 저장할 버킷이 중복되는 현상이라고도 볼 수 있다.
	- 문제점: 충돌이 많아질 수록 같은 해시값의 데이터들에서 찾아야 할 데이터에 대한 탐색 과정이 추가되어 시간 복잡도가 O(1)에서 O(n)에 가까워질 수 있다. (충돌이 적을 수록 좋은 해시 함수가 된다.)
	- 해결방안: 
		- Chaining 기법: 같은 해시 값을 갖는 데이터를 쇠사슬(chain) 모양으로 연결리스트에서 연결하여 관리하는 방법
		- Open Addressing 기법: 빈 버킷을 찾을 때까지 해시를 반복하여 관리하는 방법



### 1-2. 알고리즘

#### 1. 자바스크립트로 배열에서 중복된 값을 제거하는 방법은 무엇인가요?

1. `Set` 사용

`Set`은 ES6에서 새로 추가된 데이터 타입으로, 유일한 값을 저장하는 자료형

```js
const array = [1, 2, 2, 3, 3, 3, 4, 4, 4, 4];
const uniqueArray = [...new Set(array)]; // [1, 2, 3, 4]
```
2. `filter()`와 `indexOf()` 사용

```js
const array = [1, 2, 2, 3, 3, 3, 4, 4, 4, 4];
const uniqueArray = array.filter((value, idx, arr) => arr.indexOf(value) === idx);
```

3. `reduce()` 사용

```js
const array = [1, 2, 2, 3, 3, 3, 4, 4, 4, 4];
const uniqueArray = array.reduce((unique, item) => {
	return unique.includes(item) ? unique : [...unique, item];
}, []);
```

#### 2. 브라우저에서 가장 자주 사용하는 정렬 알고리즘의 종류와 시간 복잡도를 설명하세요.

- **Timsort** 알고리즘 ❓❓❓
	- 특징: Merge Sort와 Insertion Sort의 장점을 결합한 정렬 알고리즘이다.
	- 장점: 이미 정렬된 부분이 있는 경우 효율적이다. 실제 사용 환경에 최적화되어 있다.
	- 단점: 최악의 경우 O(n*log(n))의 시간 복잡도를 가진다.

- Merge Sort 알고리즘
	- 특징: 

- Insertion Sort 알고리즘
	- 특징: 

#### 3. 재귀(Recursion)와 반복(Iteration)의 차이와 각 방식의 장단점은 무엇인가요?

| 방식	| 특징 | 장점 | 단점 |
| --- | --- | --- | --- |
| 재귀 (Recursion) | 함수가 자기 자신을 호출한다. | 코드가 간결해진다.<br />문제를 최소 단위로 분할하여 해결하기 쉬움 | 재귀 호출이 깊어지면 스택 오버플로우 (Stack Overflow) 발생 위험이 있다.<br />성능 측면에서 오버헤드가 발생할 수 있다. |
| 반복 (Iteration) | 반복문을 사용한 순회를 통해 문제를 해결한다. | 메모리 사용 측면에서 효율적이다.<br />재귀에 비해 성능 오버헤드가 적다. | 코드가 길어질 수 있다.<br /> 재귀보다 문제의 구조적 표현이 어려울 수 있다. |

* 반복이 재귀보다 메모리 사용에 효율적인 이유는?
* 스택 오버플로우(Stack Overflow)란?
* 오버헤드(Overhead)란? ❓

## 2. 네트워크

### 1. HTTP와 HTTPS의 차이점은 무엇인가요?

- HTTP: 데이터를 평문으로 전송하는 프로토콜 (취약한 보안)
- HTTPS: (= HTTP + SSL/TSL), 데이터를 암호화된 형태로 전송함으로써 중간자 공격 등 보안 위협을 줄임

* HTTP 1, 2, 3 차이
* TCP
* UDP
* 보안 위협 중 중간자 공격이란?

### 2. HTTP 메서드(GET, POST, PUT, DELETE 등)의 차이점과 사용 사례는 무엇인가요?

| 메서드 | GET | POST | PUT | DELETE | PATCH |
| --- | --- | --- | --- | --- | --- |
| **목적과 특징** | 데이터 조회<br />(요청 파라미터가 URL에 포함됨) | 데이터 생성<br />(요청 본문에 데이터를 담아 전송) | 데이터 업데이트 | 데이터 삭제 | 부분 업데이트 시 사용 |
| **사용 예시** | 게시글 목록 조회 | 새 게시글 작성 | 전체 사용자 정보 갱신 | 특정 게시글 삭제 | - |


### 3. 브라우저에서 URL을 입력하고 Enter를 누르면 발생하는 일련의 과정을 설명하세요.

1. DNS 조회: 도메인 이름을 IP 주소로 변환
2. TCP 연결: 서버와의 연결을 수립 (HTTPS의 경우 TLS 핸드쉐이크 포함)
3. HTTP 요청: 브라우저가 서버에 요청을 보냄
3. 서버 응답: 서버가 요청에 대한 응답을 보냄 (HTML, CSS, JS 등의 리소스 포함)
4. 렌더링 (CRP, Critical Rendering Path)
	- 브라우저가 HTML을 파싱하여 DOM 생성, CSS 파싱하여 CSSOM 생성
	- DOM + CSSOM 결합하여 렌더트리 형성
	- 레이아웃
	- 페인트

### 4. DNS(Domain Name System)란 무엇이며, DNS의 역할은 무엇인가요?

- 설명: 도메인 이름을 IP 주소로 변환하여 주는 시스템을 말하며, 도메인 이름 시스템 레코드는 DNS 서버 내에서 도메인 이름을 인터넷 프로토콜(IP) 주소와 연결하는 데 사용되는 일련의 지침이다.
- 역할: 도메인 이름을 IP 주소로 변환하여, 클라이언트가 올바른 서버에 연결할 수 있도록 돕는 역할을 한다.

### 5. REST API란 무엇이며, 장단점은 무엇인가요?

- REST: 리소스 기반의 API 설계를 뜻한다. URL마다 고정된 데이터 구조를 가지고 있으며, 여러 엔드 포인트를 사용한다.
- 장점: 단순하고 캐싱, HTTP 메서드 활용 쉬움
- 단점: 필요한 데이터 이상을 가져올 수 있어 과다 응답(over-fetching) 문제가 발생할 수 있음

## 3. 브라우저와 웹 관련 이론

### 1. DOM이 무엇이고, 브라우저에서 DOM이 어떻게 작동하는지 설명하세요.

- DOM(Document Object Model)이란, HTML 문서를 객체 모델로 표현한 것으로, 각 요소가 노드로 구성되어 계층 구조를 이룬다.
- 브라우저가 HTML을 파싱하여 DOM 트리를 생성한 후, 이를 기반으로 화면에 렌더링하고 자바스크립트를 통해 동적으로 요소를 조작할 수 있다.

* HTML 파싱 과정? 
* 자바스크립트가 DOM 요소를 동적으로 조작하는 방식? 

### 2. 브라우저의 렌더링 과정에 대해 설명하세요.

- HTML 파싱: HTML 파싱하여 DOM(Document Object Model) 생성
- CSS 파싱: CSS 파싱하여 CSSOM(CSS Object Model) 생성
- 렌더 트리 구성: DOM과 CSSOM 결합하여 렌더트리를 만든다.
- 레이아웃: 렌더 트리 각 요소의 위치와 크기를 계산한다.
- 페인트: 계산된 정보를 바탕으로 픽셀 단위로 화면에 그린다.

### 3. 쿠키(Cookie), 세션(Session), 로컬 스토리지(Local Storage)의 차이점은 무엇인가요?
### 4. 이벤트 버블링(Event Bubbling)과 캡처링(Event Capturing)에 대해 설명하고, 이를 제어하는 방법은 무엇인가요?
### 5. 웹 보안과 관련하여 CSRF(Cross-Site Request Forgery)와 XSS(Cross-Site Scripting)를 방지하는 방법은 무엇인가요?

## 4. 운영 체제와 컴퓨터 구조
### 1. 스레드(Thread)와 프로세스(Process)의 차이는 무엇인가요?
### 2. 비동기(Asynchronous)와 동기(Synchronous)의 차이를 설명하세요.
### 3. 이벤트 루프(Event Loop)가 무엇이며, 자바스크립트에서 어떻게 작동하나요?
### 4. 메모리 관리에서 스택(Stack)과 힙(Heap)의 차이는 무엇인가요?
### 5. 브라우저에서 Garbage Collection이 어떻게 작동하는지 설명하세요.

## 5. 데이터베이스
### 1. 관계형 데이터베이스(Relational Database)와 NoSQL 데이터베이스의 차이점은 무엇인가요?
### 2. 데이터베이스에서 인덱스(Index)는 무엇이며, 어떻게 동작하나요?
### 3. SQL과 NoSQL 데이터베이스 중 프론트엔드 프로젝트에서 각각 적합한 사례를 들어보세요.

## 6. 기타 기초 지식
### 1. 함수형 프로그래밍(Function Programming)과 객체지향 프로그래밍(Object-Oriented Programming)의 차이점을 설명하세요.
### 2. API(Application Programming Interface)가 무엇인지, 이를 사용하는 방법은 무엇인가요?
### 3. 소프트웨어 개발에서 CORS(Cross-Origin Resource Sharing)가 왜 중요한지 설명하세요.
### 4. 빅오(Big-O) 표기법이 무엇이고, 시간 복잡도와 공간 복잡도에 대해 설명하세요.
### 5. 자바스크립트의 이벤트 위임(Event Delegation)이 무엇이고, 이를 활용하는 이유는 무엇인가요?


# FE 기술 질문

## JavaScript/TypeScript

### 1. JavaScript의 클로저(closure)와 이를 사용하는 사례를 설명하세요.
### 2. TypeScript의 주요 특징과 장점은 무엇인가요?
### 3. JavaScript에서 this 키워드가 어떻게 동작하는지 설명하세요.
### 4. ES6 이상에서의 주요 변화(예: let, const, 화살표 함수 등)에 대해 설명해 주세요.

## HTML/CSS

### 1. HTML5의 주요 특징과 브라우저 간 호환성 문제를 해결하는 방법은 무엇인가요?
### 2. Flexbox와 Grid의 차이점과 활용 사례를 비교해 주세요.
### 3. 반응형 웹 디자인을 구현할 때 고려해야 할 주요 사항은 무엇인가요?

## React/Vue

### 1. React의 상태 관리(state management)와 props의 차이를 설명하세요.
### 2. Vue의 v-model과 React의 양방향 데이터 바인딩 방식의 차이는 무엇인가요?
### 3. React에서 렌더링 성능을 최적화하기 위한 방법은 무엇인가요?

## 브라우저 동작 원리

### 1. 브라우저 렌더링 프로세스에 대해 설명하세요.
### 2. 크로스 브라우저 이슈란 무엇이며, 크로스 브라우저 이슈의 해결방법은 무엇인가요?
### 3. CORS(Cross-Origin Resource Sharing)가 무엇인지 설명하고, 이를 해결하는 방법은 무엇인가요?

# 실무 경험 및 도구

1. GitLab이나 GitHub Actions를 사용하여 CI/CD 파이프라인을 설계한 경험을 설명하세요.
2. DX(Developer Experience)를 개선하기 위한 도구를 개발한 사례가 있나요?
3. 디자인 시스템을 구축하거나 사용한 경험에 대해 설명하세요.
4. Agile/Scrum 기반 프로젝트를 진행한 경험과 역할은 무엇이었나요?

# 문제 해결 및 커뮤니케이션

1. 프로젝트에서 발생했던 가장 어려운 문제를 설명하고, 이를 어떻게 해결했는지 알려 주세요.
2. 비개발 팀(디자인, PM 등)과 협업할 때 겪은 문제와 해결 방법은 무엇인가요?
3. 코드 리뷰에서 품질을 높이기 위해 고려해야 할 사항은 무엇인가요?

# 추가

1. CI/CD 구축에서 가장 큰 도전 과제는 무엇이었나요?
2. 브라우저 성능 최적화를 위해 어떤 접근 방식을 사용하시나요?
3. 영어로 업무를 진행한 경험이 있나요? 있다면 예를 들어 주세요.
